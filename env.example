# Environment Variables
# ============================================================================
# Copy this file to .env and fill in the values for your local setup.
#
# Quick Start:
# 1. Copy this file: cp env.example .env
# 2. Start PostgreSQL: docker-compose up -d postgres
# 3. Run migrations: npx prisma migrate dev
# 4. Start dev server: npm run dev


# Database
# ============================================================================
# PostgreSQL connection string
# Default credentials match docker-compose.yml
DATABASE_URL="postgresql://sphinx_bounties:bounties_password@localhost:5433/sphinx_bounties"

# ============================================================================
# Server-side environment variables (only on server)
# These are NOT exposed to the browser

# JWT Configuration
JWT_SECRET="your-super-secret-256-bit-key-change-this-in-production"
JWT_EXPIRY_HOURS="168"

# Super Admins (comma-separated pubkeys)
SUPER_ADMINS=""

# Redis (optional - for challenge storage)
REDIS_URL=""


# ============================================================================
# Client-side environment variables (exposed to browser)
# Must be prefixed with NEXT_PUBLIC_

NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Local Mock Auth (DEV ONLY)
NEXT_PUBLIC_MOCK_USER_PUBKEY="02a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2"

