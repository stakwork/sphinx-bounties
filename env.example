# Environment Variables
# ============================================================================
# Copy this file to .env.local and fill in the values for your local setup.

# ============================================================================
# Client-side environment variables (exposed to browser)
# Must be prefixed with NEXT_PUBLIC_

# ============================================================================
# Server-side environment variables (only on server)
# These are NOT exposed to the browser

DATABASE_URL="postgresql://sphinx_bounties:bounties_password@localhost:5432/sphinx_bounties"
